// Matrix (undone)

const matrix: number[][] = [];
const n: number = matrix.length;

// Reverse list (undone)

// Built-in method
{
  const numbers: number[] = [3, 4, 5, 6, 7].reverse();
  console.log(numbers);
}
// own method
