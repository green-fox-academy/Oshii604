`use strict`;

const firstList: number[] = [1, 2, 3];
const secondList: number[] = [4, 5, 6, 9];

if (firstList.length < secondList.length) {
  console.log(`p2 is longer`);
} else {
  console.log(`p1 is longer`);
}
