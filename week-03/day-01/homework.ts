'use strict';

let hinoki : {type: string, leafColor: string, age: number, sex: string};
hinoki = {
    'type': 'hinoki wood',
    'leafColor': 'green',
    'age': 50,
    'sex': 'male',
}

let  birch: {type: string, leafColor: string, age: number, sex: string};
birch = {
    'type': 'birch',
    'leafColor': 'yellow',
    'age': 20,
    'sex': 'male',
}

let  satinWood: {type: string, leafColor: string, age: number, sex: string};
satinWood = {
    'type': 'satinwood',
    'leafColor': 'green',
    'age': 35,
    'sex': 'female',
}

let  cypress: {type: string, leafColor: string, age: number, sex: string};
cypress = {
    'type': 'cypress',
    'leafColor': 'green',
    'age': 70,
    'sex': 'male',
}

let  blackSpruce: {type: string, leafColor: string, age: number, sex: string};
blackSpruce = {
    'type': 'black spruce',
    'leafColor': 'green',
    'age': 12,
    'sex': 'female',
}